import * as React from 'react';
import styled from 'styled-components';

const Styled{{capitalize componentName}} = styled.div`
  background: red;
`;

export interface {{capitalize componentName}}Props {}

export const {{capitalize componentName}}: React.FC<{{capitalize componentName}}Props> = () => {
  return <Styled{{capitalize componentName}} />
}
